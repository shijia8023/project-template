export default {
	//暂时没用到，使用到的时候，建议配置具体路径，否则会出现一些性能问题
	//这里是发起工作使用的路径配置
	/*"msg-review": (resolve) => {
		if(globalConfig.hasFlow) {
			let path = "flow/work/review";
			return import(`@/pages/${path}.vue`);
		}
	},
	"msg-adjust": (resolve) => {
		if(globalConfig.hasFlow) {
			let path = "flow/work/write-form";
			return import(`@/pages/${path}.vue`);
		}
	}*/
}